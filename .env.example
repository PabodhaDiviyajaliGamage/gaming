# SL Gaming Hub Environment Configuration

# MongoDB Connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# Email Configuration (Required for sending order notifications)
# For Gmail: Use App Password (not your regular password)
# Enable 2-Step Verification and generate App Password at: https://myaccount.google.com/apppasswords
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password-here

# Admin Email (receives new order notifications)
ADMIN_EMAIL=admin@example.com

# API URL (uncomment and modify to override the default configuration)
# VITE_API_URL=http://slgaminghub.com/api

# Application mode (development or production)
# This will be automatically set by Vite, but you can override it here
# VITE_APP_MODE=development

# Application name
VITE_APP_NAME=SL Gaming Hub

# Enable/disable API logging (true/false)
VITE_API_LOGGING=true

# Enable/disable fallback to localStorage (true/false)
# VITE_FORCE_FALLBACK=false

